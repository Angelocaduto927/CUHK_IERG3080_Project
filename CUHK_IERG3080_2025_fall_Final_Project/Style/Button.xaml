<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    xmlns:local="clr-namespace:CUHK_IERG3080_2025_fall_Final_Project.Utility">
	<!-- Default style for all Buttons -->
	<Style TargetType="Button">
		<!-- Font -->
		<Setter Property="FontFamily" Value="Segoe UI Black"/>
		<!-- strong, game-like font -->
		<Setter Property="FontSize" Value="12"/>
		<Setter Property="FontWeight" Value="Bold"/>
		<!-- Colors -->
		<Setter Property="Background" Value="#FF1E3A8A"/>
		<!-- deep royal blue -->
		<Setter Property="Foreground" Value="#FFFFD700"/>
		<!-- bright gold text -->
		<Setter Property="BorderBrush" Value="#FFFFD700"/>
		<!-- gold border -->
		<Setter Property="BorderThickness" Value="3"/>
		<!-- thicker border -->
		<!-- Layout -->
		<Setter Property="Padding" Value="10,6"/>
		<Setter Property="Cursor" Value="Hand"/>
		<Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
		<Setter Property="RenderTransform">
			<Setter.Value>
				<ScaleTransform ScaleX="1" ScaleY="1"/>
			</Setter.Value>
		</Setter>

		<!-- Enable click sound via AudioManager -->
		<Setter Property="local:AudioManager.EnableClickSound" Value="True"/>

		<!-- Sharp edges: no CornerRadius -->
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="Button">
					<Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="0"
                            Padding="{TemplateBinding Padding}">
						<ContentPresenter
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Margin="0"
                            RecognizesAccessKey="True"
                            Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}"/>
					</Border>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
		<!-- Triggers -->
		<Style.Triggers>
			<!-- Hover -->
			<Trigger Property="IsMouseOver" Value="True">
				<Setter Property="Background" Value="#FF3B82F6"/>
				<!-- lighter blue -->
				<Setter Property="Foreground" Value="#FFFFFFE0"/>
				<!-- brighter gold-white -->
				<Setter Property="BorderBrush" Value="#FFFFFFE0"/>
				<Setter Property="Effect">
					<Setter.Value>
						<DropShadowEffect
                            Color="#FFFFD700"
                            BlurRadius="15"
                            ShadowDepth="0"
                            Opacity="0.8"/>
					</Setter.Value>
				</Setter>
				<Setter Property="RenderTransform">
					<Setter.Value>
						<ScaleTransform ScaleX="1.05" ScaleY="1.05"/>
					</Setter.Value>
				</Setter>
			</Trigger>
			<!-- Pressed -->
			<Trigger Property="IsPressed" Value="True">
				<Setter Property="RenderTransform">
					<Setter.Value>
						<ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
					</Setter.Value>
				</Setter>
				<Setter Property="Background" Value="#FF0F245C"/>
				<!-- darker royal blue -->
			</Trigger>
			<!-- Disabled -->
			<Trigger Property="IsEnabled" Value="False">
				<Setter Property="Opacity" Value="0.45"/>
				<Setter Property="Cursor" Value="Arrow"/>
			</Trigger>
		</Style.Triggers>
	</Style>
</ResourceDictionary>