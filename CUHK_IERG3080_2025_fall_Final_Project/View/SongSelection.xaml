<UserControl x:Class="CUHK_IERG3080_2025_fall_Final_Project.View.SongSelection"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800"
             Padding="16">

    <UserControl.Background>
        <ImageBrush ImageSource="/Assets/Image/SongSelectBG.png"
                    Stretch="UniformToFill" />
    </UserControl.Background>

    <UserControl.Resources>
        <!-- Style for Disabled Play Button -->
        <Style TargetType="Button" x:Key="DisabledButtonStyle">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsEnabled}" Value="False">
                    <Setter Property="Background" Value="#D3D3D3"/>
                    <!-- Gray color -->
                    <Setter Property="Foreground" Value="#A9A9A9"/>
                    <!-- Darker gray text -->
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!-- Title -->
            <RowDefinition Height="*" />
            <!-- Center cards -->
            <RowDefinition Height="Auto" />
            <!-- Footer (Back / Play) -->
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Text="Song Selection"
                   Foreground="#FFFFFF"
                   FontSize="24"
                   FontWeight="SemiBold"
                   HorizontalAlignment="Center"
                   Margin="0,4,0,12" />

        <!-- Centered cards -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
            <!-- Left card: image used as card background -->
            <Border Width="140"
                    Height="280"
                    Margin="0,0,16,0"
                    CornerRadius="8"
                    Padding="12"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                <Border.Background>
                    <ImageBrush ImageSource="/Assets/Song/Utada_Hikaru_-_One_Last_Kiss_Cover.png" Stretch="UniformToFill" />
                </Border.Background>

                <!-- Overlay + content -->
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="145*"/>
                        <RowDefinition Height="111*"/>
                    </Grid.RowDefinitions>
                    <!-- Dark overlay so text/buttons are readable over image -->
                    <Rectangle Fill="#40000000" RadiusX="6" RadiusY="6" Grid.RowSpan="2" />

                    <Grid Grid.RowSpan="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                      
                            <TextBlock Grid.Row="0"
                                   Text="One Last Kiss"
                                   Foreground="#FFFFFF"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,8" />
                        <Image Source="/Assets/Song/Utada_Hikaru_-_One_Last_Kiss_Cover.png"
                                Width="100"
                                Height="100"
                                Stretch="UniformToFill"
                                Margin="0,30,0,0"/>
                        <Grid Grid.Row="1" />
                        <StackPanel Grid.Row="2"
                                    Orientation="Vertical"
                                    HorizontalAlignment="Center"
                                    Margin="0,8,0,0">

                            <ToggleButton Content="EASY"
                                          Width="96"
                                          Margin="0,0,0,8"
                                          Padding="6,4"
                                          FontWeight="Bold"
                                          Background="#40000000"
                                          Foreground="#C0000000"
                                          IsChecked="{Binding IsEasyASelected, Mode=TwoWay}" />

                            <ToggleButton Content="HARD"
                                          Width="96"
                                          Padding="6,4"
                                          FontWeight="Bold"
                                          Background="#40000000"
                                          Foreground="#C0000000"
                                          IsChecked="{Binding IsHardASelected, Mode=TwoWay}" />
                        </StackPanel>

                    </Grid>
                </Grid>
            </Border>

            <!-- Right card: image background -->
            <Border Width="140"
                    Height="280"
                    Margin="0"
                    CornerRadius="8"
                    Padding="12"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                <Border.Background>
                    <ImageBrush ImageSource="/Assets/Song/Kenshi_Yonezu_-_Iris_Out_Cover.png" Stretch="UniformToFill" />
                    
                </Border.Background>

                <Grid>
                    <Rectangle Fill="#40000000" RadiusX="6" RadiusY="6" />

                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0"
                                   Text="Song Title B"
                                   Foreground="#FFFFFF"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,8" />
                        <Image Source="/Assets/Song/Kenshi_Yonezu_-_Iris_Out_Cover.png"
                                   Width="100"
                                   Height="100"
                                   Stretch="UniformToFill"
                                   Margin="0,30,0,0"/>
                        <Grid Grid.Row="1" />

                        <StackPanel Grid.Row="2"
                                    Orientation="Vertical"
                                    HorizontalAlignment="Center"
                                    Margin="0,8,0,0">

                            <ToggleButton Content="EASY"
                                          Width="96"
                                          Margin="0,0,0,8"
                                          Padding="6,4"
                                          FontWeight="Bold"
                                          Background="#40000000"
                                          Foreground="#C0000000"
                                          IsChecked="{Binding IsEasyBSelected, Mode=TwoWay}" />

                            <ToggleButton Content="HARD"
                                          Width="96"
                                          Padding="6,4"
                                          FontWeight="Bold"
                                          Background="#40000000"
                                          Foreground="#C0000000"
                                          IsChecked="{Binding IsHardBSelected, Mode=TwoWay}" />
                        </StackPanel>

                    </Grid>
                </Grid>
            </Border>
        </StackPanel>

        <!-- Footer with Back and Play buttons bound to NavigationVM commands on the window DataContext -->
        <Grid Grid.Row="2" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" />

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Button Content="BACK"
                        Width="96"
                        Height="36"
                        Margin="0,0,16,0"
                        HorizontalAlignment="Center"
                        Command="{Binding DataContext.TitleScreenCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                        ToolTip="Back to title" />

                <Button Content="PLAY"
                        Width="96"
                        Height="36"
                        HorizontalAlignment="Center"
                        Command="{Binding DataContext.InGameCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                        ToolTip="Start game"
                        IsEnabled="{Binding IsPlayButtonEnabled}" />
            </StackPanel>

            <Grid Grid.Column="3" />
        </Grid>

    </Grid>
</UserControl>
